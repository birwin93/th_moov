<%= form_for(@event_membership) do |f| %>
  <% if @event_membership.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event_membership.errors.count, "error") %> prohibited this event_membership from being saved:</h2>

      <ul>
      <% @event_membership.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :only %><br />
    <%= f.text_field :only %>
  </div>
  <div class="field">
    <%= f.label :[ %><br />
    <%= f.text_field :[ %>
  </div>
  <div class="field">
    <%= f.label : %><br />
    <%= f.text_field : %>
  </div>
  <div class="field">
    <%= f.label : %><br />
    <%= f.text_field : %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
