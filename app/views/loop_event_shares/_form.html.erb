<%= form_for(@loop_event_share) do |f| %>
  <% if @loop_event_share.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@loop_event_share.errors.count, "error") %> prohibited this loop_event_share from being saved:</h2>

      <ul>
      <% @loop_event_share.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
